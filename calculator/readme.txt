My Steps in Building this Calculator (JS Practice)

1. Build the HTML Structure
2. Optional: Create the CSS
3. Build the Javascript

Build the HTML Structure
a. form input for the calculator screen
b. buttons 

Build the Javascript
a. create the immediately invoked function
b. declare the dom objects such as the screen, buttons, clear, and equal btn
c. get the target values once the button is clicked and display it to the screen
d. once equal button is clicked the screen value must be evaluated
e. once clear button is clicked the screen value must return blank
